export const metadata = { title: "Attempt history", robots: { index: false, follow: false } };

export default function AttemptsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
