export const metadata = { title: "Saved tests", robots: { index: false, follow: false } };

export default function SavedTestsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
